module.exports = class Bird {
  setHeight(height) {
    this.height = height;
  }

  getHeight() {
    return this.height;
  }

  setLocation(location) {
    this.location = location;
  }

  getLocation() {
    return this.location;
  }
}
